import { ConfirmPassValidatorDirective } from './confirm-pass-validator.directive';

describe('ConfirmPassValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPassValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
